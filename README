Getting Started
---------------

1. Clone project:
  
  > git clone git://github.com/indraastra/clojure-playground.git  

2. Set up and update programming-clojure submodule:

  > git submodule init
  > git submodule update

3. Start REPL:

  > cd /path/to/clone
  > bin/clojure
